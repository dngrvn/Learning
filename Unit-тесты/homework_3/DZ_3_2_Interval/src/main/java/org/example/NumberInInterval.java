package org.example;

public class NumberInInterval
{
    public boolean numberInInterval(int n)
    {
        if(n>=25 && n<=100)
        {
            return true;
        }
        return false;
    }
}
